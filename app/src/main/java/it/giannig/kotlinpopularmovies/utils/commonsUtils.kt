package it.giannig.kotlinpopularmovies.utils

import android.content.Context
import android.support.annotation.IdRes
import android.view.View

/**
 * Created by giannig on 20/11/17.
 */
//
////Functions can have generic parameters which are specified
//// using angle brackets before the function name
//@SuppressWarnings("UNCHECKED_CAST")
//fun <T : View> Context.bind(@IdRes res: Int, ctx: Context): Lazy<T> {
//    return lazy { this.findViewById(res) as T }
//}
